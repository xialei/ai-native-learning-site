# 写作与审计规范

为保证每一章都能让读者真正"读懂"而非"读毕",所有核心章节在定稿前需过一遍下面的检查。它们的共同思路是**以终为始**:先定读者该达到什么状态,再倒推内容和图是否足以支撑。

## 一、章节内容审计(逐章,四步闭环)

对站点每一个核心章节:

1. **倒推学习目标** —— 写出 3-5 条,格式为"读完这节应该能回答/判断出什么"。
2. **倒推自测题** —— 基于学习目标出 3-5 道。**不要只复述定义**,要能检验读者是否理解了因果和权衡,而不是背下了术语。
3. **检查内容是否支撑答题** —— 如果不足以支撑读者答对自测题,标注缺了什么铺垫。常见三类缺口:
   - 用了未定义的术语;
   - 跳过了一步推导;
   - 缺少一个对比案例。
4. **检查术语引入顺序** —— 是否存在"用到某术语时,该术语还没有被定义或铺垫"的情况。

> 闭环逻辑:目标 → 自测题 → 内容是否够 → 术语是否按时到位。任何一环缺位,都说明章节还没写透。

## 二、SVG 图检查清单

本站架构图均为内联 SVG,视觉错误会直接误导读者对"谁在执行、数据怎么流"的判断。逐图核对:

1. **泳道/分层对齐** —— 若图用了泳道(swimlane)或分层标签(如按"调用方/调度层/执行层"分行),检查每个节点是否落在其声明所属的行/层内。节点位置与标签归属不一致,是最容易让人误判"这一步是谁在执行"的错误。
2. **主流程与回环/异常路径要视觉区分** —— 若存在回到前面某一步的循环箭头(重试、下一轮迭代、错误回退),检查它是否与主流程箭头交叉或重叠。要求:回环路径用虚线或不同颜色区分,并尽量绕开主流程箭头,不产生交叉误读。
3. **容器/边界框语义要明确** —— 若图中有虚线框或分组框(如表示"这几步在同一进程/服务内执行"),检查框的边界是否真的把相关节点包进去了,而不是装饰性的空框。框住的节点集合要与标签语义严格对应。
4. **颜色语义要有图例** —— 若用颜色区分了节点类型/层级(如入口层、调度层、计算层),必须在图角加图例说明每种颜色代表什么。没有图例的颜色分组一律视为需要补充。
5. **连线角度冲突** —— 检查是否存在两条独立连线因角度接近、起止点靠近,被误读成"一条折线"而非"两条独立的边"。存在则通过调整节点间距或连线路由(如改用直角折线代替斜线)区分。
6. **箭头方向与实际调用方向一致** —— 逐条核对箭头方向是否与代码中实际的调用/数据流方向一致。这是准确性而非视觉问题:若发现方向错误,**只标记不擅自修正**,在审计报告中列出,确认后再改。

## 三、移动端适配约束

站点应在手机/平板上可读,不能只在桌面浏览器下成立。逐页核对:

1. **viewport meta 必备** —— 每个 HTML `<head>` 含 `<meta name="viewport" content="width=device-width, initial-scale=1">`,否则移动端会按桌面宽度缩放,文字过小。
2. **响应式断点** —— CSS 需有 `@media` 窄屏断点:导航栏、表格、SVG `viewBox` 在小屏下不得溢出横向滚动或被截断。重点查宽表格和架构图——它们在桌面正常,在手机上最易塌。
3. **触控可点** —— 导航链接、折叠菜单的点击目标不小于约 44px,间距足够,避免误触。
4. **无桌面专属依赖** —— 不依赖 hover 才显示的关键信息(移动端无 hover),不使用固定像素宽度的主体容器。

## 四、术语一致性

避免同一概念在不同章节用不同名字,造成"以为在讲新东西"的误解。

1. **维护一份术语表** —— 全书核心术语集中登记(中文名 / 英文原名 / 别名 / 一句话定义),作为唯一基准。新增术语时先查表,确认是否已有标准译名。
2. **查别名漂移** —— 扫描全书,检查是否存在同一概念混用多个写法,例如"归约器"与"reducer 函数"混用、"节点"与"Node"中英混用不统一。发现漂移则统一到术语表的基准写法。
3. **中英混用有规可循** —— 约定哪些词保留英文(如专有名词 `AgentMessage`)、哪些统一中文。规则一旦定下,全书一致执行,不随章节作者习惯漂移。

## 五、交叉引用完整性

所有"见下一章""如前所述"之类的指代,目标必须真实存在,不能断链。

1. **正文指代可定位** —— 正文里"第 X 章""见下一节""如前所述"等指代,其目标章节/小节必须真实存在且内容确实覆盖该概念。指代到不存在的章节,或目标章节其实没讲该内容,都算断链。
2. **章节内链接有效** —— `<a href="...">` 指向的章节文件 / 锚点必须存在,无 404、无指错文件。
3. **方向自洽** —— "下一章"指代的不能是已出现过的章节,"如前所述"指代的不能是尚未讲到的内容。指代方向与实际成书顺序一致。
